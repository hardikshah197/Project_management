const userBoards = () => {};

const userBoard = (board) => {}

const getBoard = (key) => {};

const addBoard = (board) => {}

const deleteBoard = (key) => {}

const updateBoard = (key, data) => {}

export const boardService = {
    userBoards,
    getBoard,
    addBoard,
    deleteBoard,
    updateBoard,
};
